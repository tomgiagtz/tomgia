{
  "dependencies": {
    "@notionhq/client": "^1.0.4",
    "dotenv": "^16.3.1",
    "lqip": "^2.1.0",
    "moment": "^2.29.1",
    "notion-to-md": "^2.3.2"
  },
  "scripts": {
    "import": "node _scripts/notion-import.js",
    "jekyll": "bundle exec dotenv jekyll s --livereload",
    "build win": "set JEKYLL_ENV=production; bundle exec jekyll build -d docs",
    "build unix": "JEKYLL_ENV=production bundle exec jekyll build -d docs"
  }
}
